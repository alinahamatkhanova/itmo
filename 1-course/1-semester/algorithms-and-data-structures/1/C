#include <iostream>
using namespace std;

int main(){
    unsigned int a, b;
    cin >> a >> b;
    
    while (a!=b){
        if (a>b)
            a = a - b;
        if (b>a)
            b = b - a;
    }
    
    cout << a << endl;
    
    return 0;
}
